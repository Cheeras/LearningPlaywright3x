//Object annotation
let user: { name: string; age: number; isAdmin: boolean } = {
    name: "Chirag",
    age: 30,
    isAdmin: true
};
console.log(user);
console.log(`Name: ${user.name}, Age: ${user.age}, Is Admin: ${user.isAdmin}`);