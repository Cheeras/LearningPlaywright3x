
1. Before executing the Javascript program javascript will internally create Global Execution Context in 
Call Stack 
2. Execution context contains Memory and Code

Important Notes:
JavaScript Episode 1
====================
Every thing in Javascript happens inside an Execution Context

You can assume this Execution context as container or big box in which whole JavaScript code is executed

Execution Context is like big box and it has 2 components in it 

1. Memory component - This is the place where all the variable and functions are stored as key value pair
    There is also heavy word for this memory component is called Variabld environment

    Example:
    key : value
    a : 10
    fn : {}

2. Code Component : This is place where code is execute one line at a time 
                    Just like Memory componet there is also heavy word for this code component is called Thread of Execution


JavaScript is synchronous Single-threaded language:

when i say single threaded means at time JavaScript will one line at a time
synchronous single threaded means one command at a time in a specific order
that means it will go to next line once current line has been finished Execution that means synchronous 
single threaded language

And Trust me these things really very Important and no body teachs them but I here on mission to make you fall in 
love with JavaScript and don't worry continue along with me and we will be covering Every thing in deep

You can refer the Execution Context in ExecutionContext.png

===============================================================

JavaScript Episode 2:
====================

Do you know when you run JavaScript Program there are lot of things happens behinds sceens inside the JavaScript engine
Will be convering thing right now just keepwatching 

JavaScript is not possible with out these beautiful Execution context

What happens when you run javascript program, yes you are wright Execution Context is created

so let now see how this beautiful Execution Context is created with help of JavaScript program

Example Program 
===============
var n = 2;
function square(num){
    var ans = num * num;
    return ans;
}

var square2 = square(n);
var square4 = square(4);

Now let us see how this javascript code runs behind the sceensm, when you run whole code 
Global Execution Context is created and it like big box which has 2 component 
one is Memory component and Code Component, Now Listet to very care full Now ExecutionContext is created
in two phase first phase is called Memory Creation phase after words Code Execution phase

Let us first see the first see how this phase is done
==============================
Phase 1: Memory Creation Phase
==============================
If we have this code(above code) in first phase of Memory Creation Javascript will allocate memory for all the variables and functions in side
Memory component 

Step 1:
so as soon as JavaScript encouters the line var n = 2; it allocate memory to next
as n: 

Step 2:
similary javascript will goto  next line , we have function it will allocate memory to square function 
square:

Step 3:
Now let me you what does it stores when it allocates memory to n it store special value which is known as undefined

n: undefined

Step 4:
and incase of functions,how it will allocate memory to function, literally it will copy entire code into Memory component and 
create memory for that function

square: fn {...}

Step 5: Now it will also allocate memory to square2 and square4 as they are another variables so it will again store undefined for them

square2: undefined
square4: undefined

===============================================================================================================
Now let us see what happened in phase 2 i.e Code Execution phase

==============================
Phase 2 : Code Execution Phase
==============================
Let's see how code is exeucted after the memory allocation, Now JavaScript once again runs through whole JavaScript program line by line and it 
execute the code now this is the point when all these functions and every calculation in the program is done

As soon it encouters this first line var n = 2; it actually places the 2 inside the n

till now the value of n is undefined but in the second phase of Code Execution context i.e is Code Execution phase 
this 2 value of n is now being placed actually in this place holder or identifier n 

n: remove undefined and place 2


after finishing line 1 it goes to line 2 and sees that we have nothing to do here so line number 2 to 5 nothing to execute literally because
it is function definition

then it will go to next line  line 6 is most amazing part here function invokation or calling of the function 
what do you mean by function invokation when you see function name with these paranthesis with these round brackets - it means that 
now function now been called for execution 

functions are the heart of JavaScript, and you know they behave very differntly in JavaScript than any other programing language okay

functions are Here like Mini program and when function is invoked and Mini program is invoked and all to gether new Execution Context is 
Created in side the Code Component 

***************************************Important**************************************************
Remember whole thing what ever running this whole program was inside the Global Exeuction Context
**************************************************************************************************
Now when run/call/invoke a function brand new Execution context is created let us see this is gona be interesting
Just keep following 


So here Brand new ExecutionContext is created in side code Component of the GloabExecutionContext Please refer the JavaScriptCodeExecutionFlowInExecutionContext-Full Detailed.png for more details 
in the same folder

Now let us see new ExecutionContext is having Memory and code components now again be 2 phases 

1. Memory Creation Phase 
2. Code Execution 

first phase is Memory Creation Phase 
Now are talking about only following function invokation and we will be concered about this piece of code only when we are exeuting square(n)

function square(num){
    var ans = num * num;
    return ans;
}


In Phase one don't forget Memory is being allocated for variables and functions in side Memory Component it involves parameters num 
and other variable

we will be allocating memory for num and ans here in this case

Memory

num: undefined

ans: undefined

Remember what will be stored during the memory allocation phase yes you are write it is undefined 


Now comes part of Phase2 - Code Execution
=========================================
  In this we will executing each line, what happens when we execute this code when this function is invoked the value of n is passed to num 
  of square function 

  that means num: 2 , num is called parameter and n is called the argument

  value of n is 2 right now

  the code will moved to next line 

  in this line it will exeucte the calculation 
   
   num * num and put the result value into ans

   its like this it it is executing the code inside the code block of the function specific ExecutionContext

   num * num = 2 * 2 = 4 will be replaced by undefined for the ans and ans now become 4

   ans : 4

   in the next line we have encoutered special keyword called return so when ever you will see return ans this return keyword tell this
    function that you are done with work now just return the whole control back to ExecutionContext where the function was invoked

    Remember this is very Important return keyword says that return the control of the program to  the place where the function was
    invoked

    where the function was invoked

    var square2 = square2(n);// it will return to here now the control goes here

    How it happens as soon as return ans;

    it find the value of ans in local Memory and it has 4 and this return ans control back to the 

    square2 = replaceing the undefined with 4 

    Once the function exeuction is completed the whole ExecutionContext of the function will be deleted from the CallStack 

    in the next line we are again invoking square(4) only change in the argument again branch new ExecutionContext will be created 
    for this function invokation

    again New ExecutionContext will have 2 phases 

    And the whole process will repeate for the same and return value as 16

    =============================================================================

    Don't you think all  this is too much to manage to JavaScript engine like 
    ExecutionContext created inside then again one more ExecutionContext inside Code Component
    and soon creation and deletion all these things are tough  to manage right 

    and suppose there Multiple function calling side it can go any deep level function invokation 
    it is very difficult to manage and it does it very beautiful

    And it handles every thing creation and deletion of the ExecutionContext and the control

    This is  known as CallStack 

    what is callstack is like Stack 

    Every time in the bottom of stack we have GloabExecutionContext that mean's when
    ever any JavaScript program is run this callstack will be populated with this GloabExecutionContext 

    Whole execution context is pushed in side the stack and when ever new function  invoked or new ExecutionContext is created
    and this ExecutionContext is put side the stack 


    let me just denote ExecutionContext as E1 which sit on top of the GloabExecutionContext in side CallStack

    Once E1 excution is completd now this E1 is deleted or poped out of the stack and control goes back to 

    var square2 = square(n); this line

    and then we will be moved next line 

    new ExecutionContext will be created for the function invokation and E2 will be placed on top of GloabExecutionContext

    once the Execution of the E2 is completed and it will be removed or poped out of the stack return back to caller

    or GloabExecutionContext 

    And this CallStack only for managing these ExecutionContext

Once enter program exeuction is completed this GloabExecutionContext also is gone from CallStack and CallStack is empty 

And that is how whole code in side JavaScript Engine executed 

Note:
====
Call Stack maintains the order of execution of ExecutionContext

Now callStack is known very fancy names 

CallStack
ExecutionContextStack
Program Stack
Control Stack
Runtime Stack
Machine Stack

All above various names are used for this CallStack